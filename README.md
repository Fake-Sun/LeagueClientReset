# LeagueClientReset
A simple .bat file that closes the League of Legends client in a way that allows it to re-start itself. This is specially useful if you get disconnected in a lobby since the client takes longer to reconnect by itself than using this method.
